export enum RouteEnum {
  HOME = 'home',
  WORK = 'work',
  STORAGE = 'storage',
  AUTH = 'auth',
  OPTIONS = 'options',
  ABOUT = 'about',
}

export enum AuthEnum {
  LOG_IN = 'log-in',
  SIGN_UP = 'sign-up',
  FORGOT_PSWD = 'forgot-password',
}

